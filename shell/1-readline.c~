#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	char *line = NULL;
	size_t len = 0;
	ssize_t nread;

	printf("$ ");

	if ((nread = getline(&line, &len, stdin)) != -1) {
		fwrite(line, nread, 1, stdout);
	}

	free(line);
	       exit(EXIT_SUCCESS);
}
