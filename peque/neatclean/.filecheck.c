#include "main.h"

/**
 * _exit_ - exit programm
 * @stat: stat
 * @s: s
 */

int filecheck(char *filename)
{
	int i;

	for (environ
	if (stat == NULL)
	{
		free(s);
		exit(0);
	}

	else
	{
		status = atoi(stat);
		free(s);
		exit(status);
	}
}
