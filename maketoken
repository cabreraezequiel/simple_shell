#include "main.h"

/**
*
*
*
*
*/
char maketoken(char *s)
{
	char *path = getenv("PATH");
	char *tok = strtok(path, ":");
	char *con, *tokcon;

	con = str_concat("/", tok);
	tokcon = str_concat(con, s);

	while (tokcon != NULL)
	{
	}
}
